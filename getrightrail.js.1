$( document ).ready(function() {
    var pagepath = $("input[name='currentpage']").val();
    $.ajax({
        url: '/aboutus/article-rightrail.html',
        data: {
            pagepath: pagepath
        },
        success: function(response) {
           // alert("Searchresults AJAX success-" + response.success);
            $("#top-news-videos").append(response);
        },
        error: function(response) {
            //alert("Searchresults AJAX failure");

        }
    });

   /* var pagetype = $("input[name='pageIdentifier']").val();
    if(pagetype == "oldstory")
    {
        $("#oldstory").show();
    }
    else if(pagetype == "oldpress")
    {
        $("#oldpress").show();
    }
    else
    {
        $("#newarticle").show();
    }*/
});